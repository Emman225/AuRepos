name: residences_api_client
description: Client unique de l'API - dio, jeton en en-tête, enveloppe dépliée, erreurs en clair.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.10.3
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  residences_core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
