name: residences_client
description: Application Client - recherche, réservation, séjours, paiements.
publish_to: none
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  flutter_riverpod: ^2.6.1
  go_router: ^17.0.1
  residences_core:
    path: ../../packages/core
  residences_api_client:
    path: ../../packages/api_client
  residences_ui:
    path: ../../packages/ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  dio: ^5.7.0

flutter:
  uses-material-design: true
  generate: true
