/**
 * Accent réservé au SITE PUBLIC (page d'accueil, recherche, fiche logement) : jamais utilisé
 * dans le back-office ni dans l'application mobile, qui restent sur `couleurs` (jetons.ts),
 * partagé avec le thème Flutter. Un laiton chaud, dépensé avec parcimonie — notes, prix,
 * rangs, un filet d'accent — jamais comme couleur de fond ou de bordure généralisée.
 */
export const laiton = '#A9762F'
export const laitonClair = '#C99A4F'

/** Police d'affichage des grands titres du site public ; le reste du texte reste en Inter. */
export const policeDisplay = "'Newsreader', Georgia, serif"
